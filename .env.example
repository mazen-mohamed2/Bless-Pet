# Base URL for Swagger Petstore v2
VITE_API_BASE_URL=https://petstore.swagger.io/v2/

# Option 1: App-level credentials (recommended for deployed demo)
# If these are set, login checks against them and does NOT call /user/login
VITE_APP_USERNAME=demo
VITE_APP_PASSWORD=demo123